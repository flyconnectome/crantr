% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coconat-legacy.R
\name{crant_meta_legacy}
\alias{crant_meta_legacy}
\alias{crant_meta_create_cache}
\title{Legacy in-memory cache for CRANTb meta data (deprecated)}
\usage{
crant_meta_create_cache(
  use_seatable = nzchar(Sys.getenv("CRANTTABLE_TOKEN")),
  return = FALSE
)

crant_meta_legacy(ids = NULL)
}
\arguments{
\item{use_seatable}{Whether to build CRANTb meta data from the internal
seatable (development) or the \code{cell_info} CAVE table (production, never
implemented).}

\item{return}{Logical; if \code{TRUE}, return the cache tibble.}

\item{ids}{Vector of neuron/root ids to select, or \code{NULL} for all.}
}
\value{
Invisibly returns the cache (data.frame) if \code{return=TRUE};
otherwise invisibly \code{NULL}. \code{crant_meta_legacy()} returns a tibble.
}
\description{
\code{crant_meta_create_cache()} and \code{crant_meta_legacy()} reproduce the previous
closure-based metadata cache from earlier versions of crantr. They are
retained only so that existing scripts can be migrated; new code should use
\code{\link[=crant_meta]{crant_meta()}} which delegates to \code{\link[fafbseg:cam_meta]{fafbseg::cam_meta()}} and gets
transparent disk caching, delta sync, and query parsing for free.
}
\seealso{
\code{\link[=crant_meta]{crant_meta()}}
}
\keyword{internal}
