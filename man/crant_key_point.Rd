% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/key-point.R
\name{crant_key_point}
\alias{crant_key_point}
\title{Find a good "key" point on a CRANT neuron to associate with annotations}
\usage{
crant_key_point(ids, raw = TRUE, reroot = TRUE, ...)
}
\arguments{
\item{ids}{One or more CRANT root ids (anything accepted by \code{\link[=crant_ids]{crant_ids()}}).}

\item{raw}{Whether to return points in raw (voxel) space (default) or nm.}

\item{reroot}{Whether to reroot the incoming neuron onto the furthest
endpoint before simplifying.}

\item{...}{Additional arguments passed to \code{\link[=crant_read_l2skel]{crant_read_l2skel()}}.}
}
\value{
An N x 3 matrix of point locations (one row per input id). Ids whose
skeleton or key point could not be computed yield a row of \code{NA}s.
}
\description{
The chosen point sits at the major branch point of the L2
skeleton of each neuron. By default the L2 skeleton is rerooted onto the
endpoint furthest from the current root so that a simplified
representation with one branch point can be calculated; without this, the
longest path from the root may not contain a branch point at all. If no
branch point can be identified the original root point is used as a
fallback.
}
\details{
Reads an L2 skeleton for each id via \code{\link[=crant_read_l2skel]{crant_read_l2skel()}} and
picks the principal branch point with \code{\link[fafbseg:key_point_from_neuron]{fafbseg::key_point_from_neuron()}}.

Unlike the flywire and aedes datasets, the CRANT segmentation is hosted on
a separate CAVE server (proofreading.zetta.ai), which the Python \code{fafbseg}
package cannot currently target. This means \code{\link[fafbseg:read_l2skel]{fafbseg::read_l2skel()}} (and
therefore \code{\link[fafbseg:flywire_key_point]{fafbseg::flywire_key_point()}}) cannot reach it, so this function
reads via \code{crant_read_l2skel()} (pcg_skel) rather than sharing the flywire
read path. It is consequently slower than its flywire/aedes equivalent.
}
\examples{
\dontrun{
crant_key_point("576460752653449509")
}
}
\seealso{
\code{\link[fafbseg:key_point_from_neuron]{fafbseg::key_point_from_neuron()}}, \code{\link[=crant_read_l2skel]{crant_read_l2skel()}}
}
